# ERC20 转账功能 - 设计方案

## 📊 当前状态分析

### 现有合约功能

#### 1. Basic Delegation (`0x9381...2928`)
```solidity
- initialize()  // 初始化测试
- ping()        // 心跳测试
```
**特点**：简单演示合约，无实际业务逻辑

#### 2. SponsoredTransfer Delegation (`0x3bCC...9B8F`)
```solidity
- transferETH(address to, uint256 amount)                      // 单笔 ETH 转账
- batchTransfer(address[] recipients, uint256[] amounts)       // 批量 ETH 转账
- getBalance()                                                 // 查询余额
```
**特点**：支持 ETH 转账，但**不支持 ERC20 代币**

### 限制分析

#### ❌ 当前无法实现的功能
1. **ERC20 代币转账**：合约只处理 ETH，没有 ERC20 接口
2. **代币授权管理**：无法处理 ERC20 的 `approve()` / `transferFrom()`
3. **多代币支持**：无法同时处理多种 ERC20 代币
4. **代币余额查询**：只能查 ETH 余额，无法查 ERC20 余额

---

## 🎯 ERC20 转账需求

### 核心功能需求

#### 1. 单笔 ERC20 转账
```
transferERC20(address token, address to, uint256 amount)
```
- 从 Authorizer EOA 转 ERC20 代币给接收者
- Gas 由 Relay 或 Authorizer 支付（可选）

#### 2. 批量 ERC20 转账
```
batchTransferERC20(address token, address[] recipients, uint256[] amounts)
```
- 一次交易向多个地址转账同一种代币
- **核心优势**：传统 EOA 需要多笔交易，EIP-7702 只需一笔

#### 3. 多代币批量转账（高级功能）
```
multiTokenBatchTransfer(
    address[] tokens,
    address[] recipients,
    uint256[] amounts
)
```
- 一次交易转多种代币给多个地址
- **超级优势**：传统 EOA 不可能一笔完成

#### 4. ERC20 余额查询
```
getERC20Balance(address token) returns (uint256)
```
- 查询 EOA 的特定 ERC20 代币余额

---

## 🏗️ 技术方案设计

### 方案 A：扩展 SponsoredTransfer 合约（推荐）

#### 新增函数签名
```solidity
// 单笔 ERC20 转账
function transferERC20(
    address token,      // ERC20 代币合约地址
    address to,         // 接收地址
    uint256 amount      // 转账数量
) external;

// 批量 ERC20 转账（同一代币）
function batchTransferERC20(
    address token,           // ERC20 代币合约地址
    address[] calldata recipients,  // 接收地址列表
    uint256[] calldata amounts      // 转账数量列表
) external;

// 多代币批量转账（高级功能）
function multiTokenBatchTransfer(
    address[] calldata tokens,      // 代币地址列表
    address[] calldata recipients,  // 接收地址列表
    uint256[] calldata amounts      // 转账数量列表
) external;

// 查询 ERC20 余额
function getERC20Balance(address token) external view returns (uint256);

// 查询多个代币余额
function getMultipleBalances(address[] calldata tokens) external view returns (uint256[] memory);
```

#### 实现逻辑（伪代码）
```solidity
function transferERC20(address token, address to, uint256 amount) external {
    // 调用 ERC20 的 transfer 函数
    IERC20(token).transfer(to, amount);

    emit ERC20TransferExecuted(msg.sender, to, token, amount, tx.origin);
}

function batchTransferERC20(
    address token,
    address[] calldata recipients,
    uint256[] calldata amounts
) external {
    require(recipients.length == amounts.length, "Length mismatch");

    uint256 totalAmount = 0;
    for (uint256 i = 0; i < recipients.length; i++) {
        IERC20(token).transfer(recipients[i], amounts[i]);
        totalAmount += amounts[i];
    }

    emit ERC20BatchTransferExecuted(
        msg.sender,
        token,
        totalAmount,
        recipients.length,
        tx.origin
    );
}
```

#### 优点
- ✅ 保持架构一致性（与 ETH 转账并列）
- ✅ 复用现有合约模式
- ✅ 易于前端集成
- ✅ Gas 优化（批量操作）

#### 缺点
- ⚠️ 需要重新部署合约
- ⚠️ 用户需要重新授权
- ⚠️ 合约地址变更

---

### 方案 B：创建独立 ERC20Transfer 合约

#### 新合约结构
```solidity
contract ERC20TransferDelegation {
    // 专注于 ERC20 转账
    function transferERC20(...) external;
    function batchTransferERC20(...) external;
    function multiTokenBatchTransfer(...) external;

    // 代币相关辅助函数
    function getERC20Balance(...) external view;
    function approveToken(...) external;  // 授权代币
}
```

#### 优点
- ✅ 功能专一，职责清晰
- ✅ 不影响现有 SponsoredTransfer 合约
- ✅ 用户可选择授权不同合约（ETH vs ERC20）

#### 缺点
- ❌ 增加合约数量（3个合约）
- ❌ 用户需要切换不同合约
- ❌ UI 复杂度增加

---

## 🎨 前端 UI 设计

### 新增功能区域

#### 1. 代币选择区
```
┌─────────────────────────────────────────┐
│ 💎 选择代币类型                          │
├─────────────────────────────────────────┤
│ ○ ETH (原生代币)                         │
│ ● ERC20 代币                             │
│                                         │
│   代币合约地址:                          │
│   [0x... (输入或从列表选择)      ]       │
│                                         │
│   常用代币快捷选择:                      │
│   [USDC] [USDT] [DAI] [LINK]           │
└─────────────────────────────────────────┘
```

#### 2. ERC20 单笔转账
```
┌─────────────────────────────────────────┐
│ 💰 单笔 ERC20 转账                       │
├─────────────────────────────────────────┤
│ 代币: USDC (0x1c7D...)                  │
│ 余额: 1,000.00 USDC                     │
│                                         │
│ 接收地址: [0x...                ]       │
│ 转账数量: [100                  ] USDC  │
│                                         │
│ [执行转账]                               │
└─────────────────────────────────────────┘
```

#### 3. ERC20 批量转账
```
┌─────────────────────────────────────────┐
│ 📦 批量 ERC20 转账                       │
├─────────────────────────────────────────┤
│ 代币: USDC                               │
│                                         │
│ 接收地址列表 (每行一个):                 │
│ ┌─────────────────────────────────────┐ │
│ │0xRecipient1                         │ │
│ │0xRecipient2                         │ │
│ │0xRecipient3                         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 转账数量列表 (每行一个):                 │
│ ┌─────────────────────────────────────┐ │
│ │100                                  │ │
│ │200                                  │ │
│ │300                                  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 总计: 600 USDC                          │
│                                         │
│ [💸 执行批量转账]                        │
└─────────────────────────────────────────┘
```

#### 4. 多代币余额面板
```
┌─────────────────────────────────────────┐
│ 💼 代币余额总览                          │
├─────────────────────────────────────────┤
│ ETH:    2.5 ETH                         │
│ USDC:   1,000.00 USDC                   │
│ USDT:   500.00 USDT                     │
│ DAI:    750.00 DAI                      │
│                                         │
│ [🔄 刷新余额]                            │
└─────────────────────────────────────────┘
```

---

## 🔧 实施计划

### Phase 1: 合约开发 (Day 1-2)

#### 任务清单
- [ ] 创建 `ERC20TransferDelegation.sol` 合约
- [ ] 实现 `transferERC20()` 函数
- [ ] 实现 `batchTransferERC20()` 函数
- [ ] 实现 `getERC20Balance()` 查询函数
- [ ] 编写合约单元测试
- [ ] 部署到 Sepolia 测试网
- [ ] Etherscan 验证合约

#### 技术细节
```solidity
// ERC20 接口
interface IERC20 {
    function transfer(address to, uint256 amount) external returns (bool);
    function balanceOf(address account) external view returns (uint256);
    function decimals() external view returns (uint8);
    function symbol() external view returns (string memory);
}

// 事件定义
event ERC20TransferExecuted(
    address indexed from,
    address indexed to,
    address indexed token,
    uint256 amount,
    address gasPayer
);

event ERC20BatchTransferExecuted(
    address indexed from,
    address indexed token,
    uint256 totalAmount,
    uint256 recipientCount,
    address gasPayer
);
```

### Phase 2: 前端集成 (Day 3-4)

#### 任务清单
- [ ] 更新 `contract.ts` 配置（新增 ERC20Transfer 合约）
- [ ] 创建 `useERC20Transfer.ts` hook
- [ ] 实现代币选择器组件
- [ ] 实现 ERC20 单笔转账 UI
- [ ] 实现 ERC20 批量转账 UI
- [ ] 实现余额查询和显示
- [ ] 添加常用代币列表（USDC, USDT, DAI, LINK）
- [ ] 集成到主页面

#### 技术栈
```typescript
// Hook 示例
export const useERC20Transfer = () => {
  const transferERC20 = async (
    token: string,
    to: string,
    amount: bigint
  ) => {
    // 编码函数调用
    const data = encodeFunctionData({
      abi: erc20TransferAbi,
      functionName: 'transferERC20',
      args: [token, to, amount],
    })

    // 发送交易到 Authorizer EOA
    const hash = await walletClient.sendTransaction({
      to: authorizerAddress,
      data,
      gas: 150000n,
    })

    return hash
  }

  return { transferERC20 }
}
```

### Phase 3: 测试验证 (Day 5)

#### 测试场景
1. **单笔 ERC20 转账**
   - 转账 100 USDC 给地址 A
   - 验证余额变化
   - 验证交易成功

2. **批量 ERC20 转账**
   - 转账给 3 个地址：100, 200, 300 USDC
   - 验证所有接收者余额
   - 对比 gas 成本（vs 3 笔单独交易）

3. **边界测试**
   - 余额不足时的错误处理
   - 地址数量不匹配时的验证
   - 零地址/零金额的边界情况

4. **Gas 成本分析**
   - 测量批量转账 vs 多次单笔的 gas 差异
   - 更新文档

### Phase 4: 文档和优化 (Day 6)

#### 任务清单
- [ ] 更新 README.md
- [ ] 编写 ERC20 转账教程
- [ ] Gas 成本分析文档
- [ ] 添加演示视频/截图
- [ ] 代码优化和清理
- [ ] 发布 v1.2.0

---

## 📊 预期效果

### Gas 成本对比（预估）

#### 场景：向 3 个地址转 ERC20 代币

**传统 EOA 方式**：
```
交易 1: transfer(A, 100) → 65,000 gas
交易 2: transfer(B, 200) → 65,000 gas
交易 3: transfer(C, 300) → 65,000 gas
────────────────────────────────────
总计: 195,000 gas
```

**EIP-7702 + 批量转账**：
```
交易 1: batchTransferERC20([A,B,C], [100,200,300]) → 120,000 gas
────────────────────────────────────
总计: 120,000 gas
节约: ~38% ✅
```

### 用户体验提升

| 维度 | 传统 EOA | EIP-7702 |
|------|----------|----------|
| 交易次数 | 3 笔 | 1 笔 |
| 签名次数 | 3 次 | 1 次 |
| Gas 成本 | 195k | 120k (-38%) |
| 等待时间 | ~36 秒 | ~12 秒 |
| 失败风险 | 高（3次机会） | 低（1次） |

---

## 🎯 推荐方案

### ✅ 最终推荐：方案 A（扩展 SponsoredTransfer）

**理由**：
1. **架构统一**：ETH 和 ERC20 转账在同一合约
2. **用户体验**：用户只需授权一次，可同时使用 ETH 和 ERC20 功能
3. **功能完整**：一个合约解决所有转账需求
4. **易于维护**：代码集中，逻辑清晰

**实施步骤**：
1. 创建 `SponsoredTransferDelegationV2.sol`
2. 继承 V1 的所有 ETH 功能
3. 新增 ERC20 转账功能
4. 部署到 Sepolia
5. 更新前端配置
6. 测试验证
7. 发布 v1.2.0

---

## ❓ 待确认问题

### 1. 合约部署方式
- [ ] **选项 A**：创建新合约 `SponsoredTransferV2`（推荐）
- [ ] **选项 B**：创建独立 `ERC20Transfer` 合约
- [ ] **选项 C**：使用代理合约模式（复杂度高）

### 2. 测试代币选择
建议使用 Sepolia 上的测试代币：
- [ ] USDC（或 Mock USDC）
- [ ] USDT（或 Mock USDT）
- [ ] 自己部署 Mock ERC20 代币

### 3. UI 集成方式
- [ ] **选项 A**：在现有转账区域添加代币选择（简单）
- [ ] **选项 B**：创建独立的 ERC20 转账 Tab（清晰）
- [ ] **选项 C**：统一转账界面（复杂但用户体验好）

### 4. 功能范围
第一版需要实现哪些功能？
- [x] 单笔 ERC20 转账（必须）
- [x] 批量 ERC20 转账（必须）
- [x] ERC20 余额查询（必须）
- [ ] 多代币批量转账（可选，高级功能）
- [ ] 代币授权管理（可选）
- [ ] 代币信息查询（symbol, decimals）（可选）

---

## 📋 开发检查清单

### 准备阶段
- [x] 需求分析完成
- [ ] 技术方案确认
- [ ] 测试代币准备
- [ ] 开发环境配置

### 开发阶段
- [ ] 合约编写
- [ ] 合约测试
- [ ] 合约部署
- [ ] 前端 Hook 开发
- [ ] UI 组件开发
- [ ] 集成测试

### 验证阶段
- [ ] 功能测试
- [ ] Gas 成本测试
- [ ] 边界测试
- [ ] 用户体验测试

### 发布阶段
- [ ] 文档更新
- [ ] 代码审查
- [ ] Tag 版本
- [ ] 发布说明

---

## 🚀 预期时间线

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| 设计确认 | 0.5 天 | 本文档 + 方案确认 |
| 合约开发 | 1 天 | 合约代码 + 测试 + 部署 |
| 前端开发 | 1.5 天 | UI + Hook + 集成 |
| 测试验证 | 0.5 天 | 测试报告 + Gas 分析 |
| 文档发布 | 0.5 天 | 文档 + v1.2.0 |
| **总计** | **4 天** | **ERC20 转账完整功能** |

---

## 💡 后续扩展思路

### Phase 2 功能（v1.3.0）
- NFT 批量转账（ERC721）
- NFT 批量铸造（Mint）
- 合约交互批量执行

### Phase 3 功能（v2.0.0）
- 跨链转账（Layer 2）
- DeFi 协议集成（Uniswap, Aave）
- 智能合约钱包完整功能

---

**文档版本**: v1.0
**创建日期**: 2025-01-18
**状态**: 待确认 ⏳
